export interface SocialLink {
  name: string;
  url: string;
  icon: any;
  color: string;
  hoverColor: string;
  description: string;
}

export interface LinkCounter {
  name: string;
  count: number;
}
